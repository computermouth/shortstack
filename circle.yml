machine:
  services:
    - docker

dependencies:
  post:
    - sudo apt-get update -y -qq
    - sudo apt-get install -y -qq libsdl2-dev build-essential
    - make

test:
  post:
    - cp shortstack $CIRCLE_ARTIFACTS
